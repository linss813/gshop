<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linss.gshop.mapper.BannerMapper">

    <resultMap id="BannerResultMap" type="Banner">
        <id property="bid" column="bid"/>
        <result property="image" column="image"/>
        <result property="jump" column="jump"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="getAllBanner" resultType="com.linss.gshop.entity.Banner">
        SELECT
        bid, image, jump, sort, status, created_at as createdAt
        FROM banner
        ORDER BY sort DESC
    </select>

    <select id="getBannerById" resultType="com.linss.gshop.entity.Banner" parameterType="int">
        SELECT
        bid, image, jump, sort, status, created_at as createdAt
        FROM banner
        WHERE bid = #{bid}
    </select>

    <insert id="addBanner">
        INSERT INTO banner(image, jump, sort, status, created_at)
        VALUES (#{image}, #{jump}, #{sort}, #{status}, NOW())
    </insert>

    <update id="updateBanner">
        UPDATE banner
        SET image = #{image}, jump = #{jump}, sort = #{sort}, status = #{status}
        WHERE bid = #{bid}
    </update>

    <delete id="deleteBanner">
        DELETE FROM banner
        WHERE bid = #{bid}
    </delete>

</mapper>
